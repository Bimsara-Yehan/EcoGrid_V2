# Frontend (Vite + React)

## Dev

1. Start backend (from `Backend`):
   ```bash
   npm start
   ```
2. Start frontend (from `frontend`):
   ```bash
   npm run dev
   ```

- The Vite proxy forwards `/Zones` and `/subscriptions` to `http://localhost:5000`.
- UI shows Zones and Subscriptions lists.
